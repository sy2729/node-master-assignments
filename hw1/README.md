# This is the first assignment of the Node Master Course
### Build up a basic API

### Usage
`node hw1.js`,
then the page will listen on port 3000
just input the path 'hello' to see the returned JSON data;